<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Define the background properties progress like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient android:startColor="@color/grey"
                android:centerColor="@color/grey"
                android:centerY="1.0"
                android:endColor="#a5e685"
                android:angle="270" />
        </shape>
    </item>
    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>

                <gradient

                    android:startColor="@color/theMainColor"
                    android:centerColor="@color/theMainColor"

                    android:centerY="1.0"

                    android:endColor="@color/theMainColor"


                    android:angle="270"/>


            </shape>

        </clip>

    </item>


</layer-list>